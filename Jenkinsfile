pipeline {
    agent any
    tools {
        node 'NodeJS 19.2.0'
    }
    stages {
        stage("pull code from git repo"){
            steps{
                echo "pulling code"
            }
        }
    }
}