pipeline {
    agent any
    tools {
        nodejs 'NodeJS 19.2.0'
    }
    stages {
        stage("pull code from git repo"){
            steps{
                echo "pulling code"
            }
        }
    }
}